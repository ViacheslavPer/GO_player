package runtime

// RuntimeGraph is built from BaseGraph.
// Lives only in RAM.
// Rebuildable from scratch.
type RuntimeGraph struct {
	// TODO: edges map[from][to]probability
}

// TODO: BuildFromBase(baseGraph)
